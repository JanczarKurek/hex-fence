[package]
name = "giereczka"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16.1", features = ["dynamic_linking", "wayland"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
